services:
  - type: web
    name: relieflink
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app_fixed:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
